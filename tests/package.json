{
  "scripts": {
    "test:ci": "vitest run",
    "test": "vitest",
    "pretest": "npm run buildtests",
    "pretest:ci": "npm run buildtests",
    "buildtests": "chmod +x ./buildtests && ./buildtests"
  }
}
